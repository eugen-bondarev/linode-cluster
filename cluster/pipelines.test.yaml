controller:
  JCasC:
    configScripts:
      pipeline-job: |
        jobs:
          - script: >
              job('demo1') {
                  steps {
                      shell('echo Hello World!')
                  }
              }
          - script: >
              job('demo2') {
                  steps {
                      shell('echo Hello GitOps!')
                  }
              }
